apiVersion: apps/vi
kind: apiVersion: apps/v1
kind: Deployment
metadata:
  name: home
  labels: 
    app:home
spec:
  selector:
    matchLabels:
      app: home
  template:
    metadata:
      name: home
      labels:
s       app: home
      spec:
      containers:
  
    name: home    - name: hose
  
  lamage: <Image>
        rurces:
    
      name: home      limiss:
            memory: "128Mi"
            cpu: "50  name: home        - ct: <Port>
