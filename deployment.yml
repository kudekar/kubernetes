apiVersion: apps/vi
kind: apiVersion: apps/v1
kind: Deployment
metadata:
  name: home
  labels: 
    app:home
spec:
  selector:
    matchLabels:
      app: home
  
  labels: 
    app:home
  template:
    metadata:
      labels:
s       app: home
    
    labels: 
      app:home
    spec:
      containers:
      - name: hose
  
  labels: 
    app:home
        image: <Image>
        resources:
          limiss:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: <Port>
