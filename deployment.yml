apiVersion: apps/vi
kind: apiVersion: apps/v1
kind: Deployment
metadata:
  name: home
  labels: 
    app:
spec:
  selector:
    matchLabels:
      app: home
  
  labels: 
    app:
  template:
    metadata:
      labels:
        app: home
    
    labels: 
      app:
    spec:
      containers:
      - name: home
  
  labels: 
    app:
        image: <Image>
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: <Port>
