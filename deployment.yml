apiVersion: apps/vi
kind: apiVersion: apps/v1
kind: Deployment
metadata:
  name: home
  labels: 
    apps:home
spec:
  selector :
    matchLabels:
      apps: home
  template:
    metadata:
      name: home
      labels:
        apps: home
      spec:
      containers:
      - name: myhttpd
        image: httpd
        ports:
        - containerPorts: 80
  
  lamage: <Image>
        rurces:
    
      name: home      limiss:
            memory: "128Mi"
            cpu: "50  name: home        - ct: <Port>
