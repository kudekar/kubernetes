apiVersion: apps/vi
kind: apiVersion: apps/v1
kind: Deployment
metadata:
  name: home
  labels: 
    app:home
spec:
  selector:
    matchLabels:
      app: home
  template:
    metadata:
      labels:
s       app: home
    
      spec:
      containers:
      - name: hose
  
  lamage: <Image>
        resources:
          limiss:
            memory: "128Mi"
            cpu: "500m
        - ct: <Port>
