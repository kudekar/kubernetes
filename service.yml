apiVersion: v1
kind: Service
metadata:
  name: my-svc
  labels: 
    apps: my-svc
spec:
  selector:
    app: home
  ports:
  - port: 80
    name: httpd
    targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: my-svc-2
  labels: 
    apps: Service
spec:
  selector:
    app: home2
  ports:
  - port: 80
    name: nginx
    targetPort: 80